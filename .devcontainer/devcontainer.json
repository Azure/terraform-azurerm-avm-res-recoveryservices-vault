{
  "image": "mcr.microsoft.com/azterraform:avm-latest",
  "containerEnv": {
    "AVM_IN_CONTAINER": "true"
  },
  "updateRemoteUserUID": true,
  "runArgs": [
    "--cap-add=SYS_PTRACE",
    "--security-opt",
    "seccomp=unconfined",
    "--init",
    "--network=host"
  ],
  "mounts": [
    "source=/var/run/docker.sock,target=/var/run/docker.sock,type=bind"
  ],
  "onCreateCommand": "terraform version",
  "postCreateCommand": "if [ -f .githooks/pre-commit ]; then git config core.hooksPath .githooks && chmod +x .githooks/pre-commit; else echo '.githooks/pre-commit not found; skipping hook setup'; fi",
  "customizations": {
    "vscode": {
      "settings": {
        "terraform.languageServer.terraform.path": "/home/runtimeuser/tfenv/bin/terraform"
      },
      "extensions": [
        "ms-azuretools.vscode-azureterraform",
        "EditorConfig.EditorConfig",
        "hashicorp.terraform"
      ]
    }
  }
}
